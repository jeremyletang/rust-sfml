#pragma once

typedef enum {
    sfMouseLeft,
    sfMouseRight,
    sfMouseMiddle,
    sfMouseButtonExtra1,
    sfMouseButtonExtra2,
} sfMouseButton;

typedef enum {
    sfMouseVerticalWheel,
    sfMouseHorizontalWheel
} sfMouseWheel;
